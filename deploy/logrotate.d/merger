/opt/gewuit/rfid/merger.log
{
	daily
	missingok
	rotate 30
	compress
	delaycompress
	notifempty
	sharedscripts
	postrotate
		[ ! -f /var/run/merger.pid ] || kill -USR1 `cat /var/run/merger.pid`
	endscript
}
