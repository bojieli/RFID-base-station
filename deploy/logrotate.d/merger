/opt/gewuit/rfid/log/merger.log
{
	daily
	missingok
	rotate 52
	compress
    dateext
	notifempty
	sharedscripts
	postrotate
		[ ! -f /var/run/merger.pid ] || kill -USR1 `cat /var/run/merger.pid`
	endscript
}
