/opt/gewuit/rfid/receiver.log
{
	daily
	missingok
	rotate 10
	compress
	delaycompress
	notifempty
	sharedscripts
	postrotate
		[ ! -f /var/run/receiver.pid ] || kill -USR1 `cat /var/run/receiver.pid`
	endscript
}
