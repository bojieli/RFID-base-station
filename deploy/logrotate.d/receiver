/opt/gewuit/rfid/log/receiver.log
{
	daily
	missingok
	rotate 52
	compress
    dateext
	notifempty
	sharedscripts
	postrotate
		[ ! -f /var/run/receiver.pid ] || kill -USR1 `cat /var/run/receiver.pid`
	endscript
}
